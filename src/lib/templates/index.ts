/**
 * Template type definitions.
 *
 * Note: Actual template data is stored in the database.
 * Default templates can be loaded from src/lib/seeds/inkoop-settings.json
 * via the admin panel's "Reset naar standaardwaarden inkoop" button.
 */

import { STANDARD_CHECKBOXES } from './checkboxes'

export { STANDARD_CHECKBOXES }
export * from './types'
